testing
shouldInheritSelectors
	"there are only virtual test selectors, no inheritance possible.
	Also, if this were to answer true, #testSelectors would not be
	called, which would break test case loading."
	^ false.